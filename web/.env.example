# Public URL of the site; used for canonical/OG metadata.
NEXT_PUBLIC_SITE_URL=https://navi.app

# Supabase project for the waitlist table. When both are absent the API route
# appends to web/.waitlist.local.jsonl instead (dev fallback).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
